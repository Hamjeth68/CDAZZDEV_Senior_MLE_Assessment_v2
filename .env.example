# Copy this file to `.env` and set values locally.
# Never commit populated secrets.

# Groq is the primary LLM provider.
GROQ_API_KEY=

# OpenRouter is used as fallback when configured.
OPENROUTER_API_KEY=
